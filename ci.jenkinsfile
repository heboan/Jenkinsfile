#!groovy
pipeline {
    agent { node { label "master" } }
    stages{
        stage("build"){
            steps{
                script{
                    nodeHome = tool "NPM"
                    sh "${nodeHome}/bin/npm -v"
                }
            }
        }
    }
}
